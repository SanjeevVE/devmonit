Mono Repo Architecture

A Monorepo (Monolithic Repository) is a single Git repository that contains multiple projects, such as frontend apps, backend services, shared libraries, and utilities.

Instead of maintaining many repos, everything lives in one repository.

Next.js Frontend and FastAPI backend
